<Window x:Class="MuseumApp.Windows.Exhibits.EditExhibitWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Изменить экспонат" Height="400" Width="480"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Style="{StaticResource DialogWindow}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Название:"/>
        <TextBox x:Name="txtName" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Коллекция:"/>
        <ComboBox x:Name="cmbCollection" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"
                  DisplayMemberPath="Name" SelectedValuePath="Id"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Автор:"/>
        <ComboBox x:Name="cmbAuthor" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"
                  DisplayMemberPath="Name" SelectedValuePath="Id"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Состояние:"/>
        <ComboBox x:Name="cmbCondition" Grid.Row="3" Grid.Column="1" Margin="0,0,0,8"
                  DisplayMemberPath="Name" SelectedValuePath="Id"/>

        <Label Grid.Row="4" Grid.Column="0" Content="Дата создания:"/>
        <DatePicker x:Name="dpCreationDate" Grid.Row="4" Grid.Column="1" Margin="0,0,0,8"/>

        <Button Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Content="Сохранить"
                Height="32" Click="btnSave_Click"/>
    </Grid>
</Window>
